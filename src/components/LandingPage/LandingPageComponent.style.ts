/* eslint-disable import/prefer-default-export */
import styled from 'styled-components'

import { Card } from '@material-ui/core'

export const DefinitionCard = styled(Card)`
  display: inline-block;
  max-width: 20rem;
  margin-right: 1rem;
  margin-bottom: 1rem;
  vertical-align: top;
`
